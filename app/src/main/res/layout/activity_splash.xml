<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="@drawable/splash_bg"
    android:layout_height="match_parent"
    tools:context=".Activity.NewApp.SplashActivity">

   <ImageView
       android:id="@+id/imgVwLogo"
       android:layout_width="match_parent"
       android:layout_height="@dimen/_100sdp"
       android:layout_centerVertical="true"
       android:src="@drawable/ic_app_logo_white"
       />
   <TextView
       android:id="@+id/tvSubText"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:text="Face recognition based attendance &amp; visitor management"
       android:layout_below="@+id/imgVwLogo"
       android:textColor="@color/white"
       android:fontFamily="@font/gotham_x_light_italic"
       android:textSize="@dimen/_14ssp"
       android:layout_marginStart="@dimen/_40sdp"
       android:layout_marginEnd="@dimen/_40sdp"
       android:gravity="center"
       />
   <TextView
       android:id="@+id/tvRole"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:text="Admin"
       android:textAllCaps="true"
       android:layout_below="@+id/tvSubText"
       android:textColor="@color/white"
       android:layout_marginTop="@dimen/_10sdp"
       android:fontFamily="@font/gotham_book_regular"
       android:textSize="@dimen/_16ssp"
       android:layout_marginStart="@dimen/_40sdp"
       android:layout_marginEnd="@dimen/_40sdp"
       android:gravity="center"
       />
</RelativeLayout>